# Set prefix key to Alt-a
unbind C-b
set-option -g prefix M-a

# Send Alt-a to the application running in the pane
bind M-a send-prefix

# change tmux bar color
set -g status-bg cyan

# Change index to start from 1
set -g base-index 1
setw -g pane-base-index 1

